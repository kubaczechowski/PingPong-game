import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class WinningPlayerImage here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WinningPlayerImage extends Actor
{
    /**
     * Act - do whatever the WinningPlayerImage wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public WinningPlayerImage(String fileName) 
    {
        setImage(fileName);
    }    
}
